using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class CustomCam : MonoBehaviour
{
    public GameObject target;

    
    // void Start()
    // {
    //     //transform.position = new Vector3(9.71f, 6.21f, -6.5f);
    //     //transform.Rotate(5f, -40f, 0f);
    //     //RenderSettings.skybox = skyMat[0];
    //     // RenderSettings.skybox = skyMat[1];
    //     // Skybox mySkyBox = new Skybox();
    //     // // Assigns a material named "Assets/Resources/DEV_Orange" to the object.
    //     // Material skybox = Resources.Load("sky_mat", typeof(Material)) as Material;
    //     // mySkyBox.material = skybox;
    // }

    // Update is called once per frame
    void FixedUpdate()
    {
            // if (target.transform.position.y<0)
            // {
            //     Vector3 targetPosition = new Vector3(target.transform.position.x, target.transform.position.y, target.transform.position.z + 6.5f);
            //     transform.LookAt(targetPosition);
            // }
        
    }
}
